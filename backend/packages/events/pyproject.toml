[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "noveland-events"
version = "0.1.0"
description = "Event and snapshot contracts package for Noveland."
requires-python = ">=3.12,<3.13"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/noveland"]
