[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "noveland-calendar"
version = "0.1.0"
description = "Private agent calendar package for Noveland."
requires-python = ">=3.12,<3.13"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/noveland"]
