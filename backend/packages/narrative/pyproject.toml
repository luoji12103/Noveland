[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "noveland-narrative"
version = "0.1.0"
description = "Narrative orchestration and artifacts package for Noveland."
requires-python = ">=3.12,<3.13"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/noveland"]
