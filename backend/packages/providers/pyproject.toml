[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "noveland-providers"
version = "0.1.0"
description = "Provider registry and execution kernel package for Noveland."
requires-python = ">=3.12,<3.13"
dependencies = [
  "noveland-core",
  "noveland-invocations",
  "noveland-media",
  "noveland-worlds",
  "pydantic>=2.13,<3",
  "sqlalchemy>=2.0,<3",
]

[tool.uv.sources]
noveland-core = { workspace = true }
noveland-invocations = { workspace = true }
noveland-media = { workspace = true }
noveland-worlds = { workspace = true }

[tool.hatch.build.targets.wheel]
packages = ["src/noveland"]
