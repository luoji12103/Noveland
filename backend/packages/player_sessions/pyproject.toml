[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "noveland-player-sessions"
version = "0.1.0"
description = "Player-owned resume and recovery state package for Noveland."
requires-python = ">=3.12,<3.13"
dependencies = [
  "noveland-conversations",
  "noveland-core",
  "noveland-media",
  "noveland-worlds",
  "pydantic>=2.13,<3",
  "sqlalchemy>=2.0,<3",
]

[tool.uv.sources]
noveland-conversations = { workspace = true }
noveland-core = { workspace = true }
noveland-media = { workspace = true }
noveland-worlds = { workspace = true }

[tool.hatch.build.targets.wheel]
packages = ["src/noveland"]
