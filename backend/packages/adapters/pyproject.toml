[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "noveland-adapters"
version = "0.1.0"
description = "Provider, storage, and transport adapter package for Noveland."
requires-python = ">=3.12,<3.13"
dependencies = [
  "httpx>=0.28,<0.29",
  "noveland-core",
  "pydantic>=2.13,<3",
  "sqlalchemy>=2.0,<3",
]

[tool.uv.sources]
noveland-core = { workspace = true }

[tool.hatch.build.targets.wheel]
packages = ["src/noveland"]
