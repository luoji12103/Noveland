[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "noveland-memory"
version = "0.1.0"
description = "Memory abstractions package for Noveland."
requires-python = ">=3.12,<3.13"
dependencies = [
  "mem0ai>=1.0,<2",
  "noveland-agents",
  "noveland-core",
  "noveland-events",
  "pydantic>=2.13,<3",
  "sqlalchemy>=2.0,<3",
]

[tool.uv.sources]
noveland-agents = { workspace = true }
noveland-core = { workspace = true }
noveland-events = { workspace = true }

[tool.hatch.build.targets.wheel]
packages = ["src/noveland"]
